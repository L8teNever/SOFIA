const API = {
  _base: '/api/v1',

  async _req(method, path, body, isForm = false) {
    const opts = { method, headers: {} };
    if (body && !isForm) { opts.headers['Content-Type'] = 'application/json'; opts.body = JSON.stringify(body); }
    if (body && isForm) opts.body = body;
    const res = await fetch(this._base + path, opts);
    if (!res.ok) {
      let detail = '';
      try { detail = (await res.json()).detail; } catch {}
      const err = new Error(detail || `${res.status}`);
      err.status = res.status;
      err.detail = detail;
      throw err;
    }
    const ct = res.headers.get('content-type') || '';
    return ct.includes('application/json') ? res.json() : res.text();
  },

  get:    (path)        => API._req('GET',    path),
  post:   (path, body)  => API._req('POST',   path, body),
  put:    (path, body)  => API._req('PUT',    path, body),
  patch:  (path, body)  => API._req('PATCH',  path, body),
  delete: (path)        => API._req('DELETE', path),
  upload: (path, form)  => API._req('POST',   path, form, true),

  me:            ()       => API.get('/auth/me'),
  users:         ()       => API.get('/users/'),
  classUsers:    ()       => API.get('/users/class'),
  createUser:    (d)      => API.post('/users/', d),
  updateMe:      (d)      => API.patch('/users/me', d),
  updateUser:    (id, d)  => API.patch(`/users/${id}`, d),
  listUserEmails: (id)       => API.get(`/users/${id}/emails`),
  addUserEmail:   (id, email) => API.post(`/users/${id}/emails`, { email }),
  deleteUserEmail: (id, aliasId) => API.delete(`/users/${id}/emails/${aliasId}`),
  uploadAvatar:  (form)   => API.upload('/users/me/avatar', form),
  deleteAvatar:  ()       => API.delete('/users/me/avatar'),
  classes:       ()       => API.get('/classes/'),
  createClass:   (d)      => API.post('/classes/', d),
  deleteClass:   (id)     => API.delete(`/classes/${id}`),
  saveUntis:     (id, d)  => API.post(`/classes/${id}/untis`, d),
  getUntis:      (id)     => API.get(`/classes/${id}/untis`),
  testUntis:      (id, d)  => API.post(`/classes/${id}/untis/test`, d),
  importSubjects:  (id)     => API.post(`/classes/${id}/untis/import-subjects`, {}),
  reconnectUntis:  (id)     => API.post(`/classes/${id}/untis/reconnect`, {}),
  uploadTimetablePhoto: (id, form) => API.upload(`/classes/${id}/timetable-photo/upload`, form),
  timetablePhotoStatus: (id)     => API.get(`/classes/${id}/timetable-photo/status`),
  getTimetableSource:   (id)     => API.get(`/classes/${id}/timetable-source`),
  setTimetableSource:   (id, d)  => API.post(`/classes/${id}/timetable-source`, d),
  listManualTimetable:   (id)          => API.get(`/classes/${id}/timetable-manual`),
  createManualTimetableEntry: (id, d)  => API.post(`/classes/${id}/timetable-manual`, d),
  updateManualTimetableEntry: (id, eId, d) => API.put(`/classes/${id}/timetable-manual/${eId}`, d),
  deleteManualTimetableEntry: (id, eId)    => API.delete(`/classes/${id}/timetable-manual/${eId}`),
  subjects:      ()       => API.get('/subjects/'),
  createSubject: (d)      => API.post('/subjects/', d),
  updateSubject: (id, d)  => API.put(`/subjects/${id}`, d),
  deleteSubject: (id)     => API.delete(`/subjects/${id}`),
  events:        (month)  => API.get('/calendar/' + (month ? `?month=${month}` : '')),
  createEvent:   (d)      => API.post('/calendar/', d),
  updateEvent:   (id, d)  => API.put(`/calendar/${id}`, d),
  deleteEvent:   (id)     => API.delete(`/calendar/${id}`),

  holidayStates: ()       => API.get('/calendar/holidays/states'),
  holidayPreview:(st, yr) => API.get(`/calendar/holidays/preview?state=${st}&year=${yr}`),
  importHolidays:(d)      => API.post('/calendar/holidays/import', d),
  deleteHolidays:(p)      => API.delete('/calendar/holidays' + (p ? `?${new URLSearchParams(p)}` : '')),
  homework:      ()       => API.get('/homework/'),
  homeworkDetail:(id)     => API.get(`/homework/${id}`),
  createHomework:(d)      => API.post('/homework/', d),
  updateHomework:(id, d)  => API.put(`/homework/${id}`, d),
  uploadHwFile:  (form)   => API.upload('/homework/upload', form),
  checkHomework: (id)     => API.post(`/homework/${id}/check`),
  deleteHomework:(id)     => API.delete(`/homework/${id}`),
  homeworkSolutions: (hwId)        => API.get(`/homework/${hwId}/solutions`),
  createSolution:    (hwId, d)     => API.post(`/homework/${hwId}/solutions`, d),
  updateSolution:    (hwId, solId, d) => API.put(`/homework/${hwId}/solutions/${solId}`, d),
  deleteSolution:    (hwId, solId) => API.delete(`/homework/${hwId}/solutions/${solId}`),
  grades:        ()       => API.get('/grades/'),
  createGrade:   (d)      => API.post('/grades/', d),
  updateGrade:   (id, d)  => API.put(`/grades/${id}`, d),
  deleteGrade:   (id)     => API.delete(`/grades/${id}`),
  timetable:     ()       => API.get('/timetable/'),
  mealplanCurrent:  ()       => API.get('/mealplan/current'),
  mealplanStatus:   ()       => API.get('/mealplan/status'),
  uploadMealPlan:   (form)   => API.upload('/mealplan/upload', form),
  updateMealDay:    (id, d)  => API.patch(`/mealplan/day/${id}`, d),
  deleteMealPlan:   (id)     => API.delete(`/mealplan/${id}`),
  appVersion:       ()       => API.get('/version'),
  files:         ()       => API.get('/files/'),
  uploadFile:    (form)   => API.upload('/files/', form),
  deleteFile:    (id)     => API.delete(`/files/${id}`),
  driveFiles:      ()       => API.get('/drive/'),
  uploadDriveFile: (form)   => API.upload('/drive/upload', form),
  updateDriveFile: (id, d)  => API.put(`/drive/${id}`, d),
  driveFileText:   (id)     => API.get(`/drive/${id}/text`),
  updateDriveFileText: (id, d) => API.put(`/drive/${id}/text`, d),
  deleteDriveFile: (id)     => API.delete(`/drive/${id}`),
  driveTopics:      (subjectId) => API.get(`/drive/topics?subject_id=${subjectId}`),
  createDriveTopic: (d)         => API.post('/drive/topics', d),
  renameDriveTopic: (id, d)     => API.put(`/drive/topics/${id}`, d),
  deleteDriveTopic: (id)        => API.delete(`/drive/topics/${id}`),
  googleStatus:     ()       => API.get('/google/status'),
  googleAuthUrl:    ()       => API.get('/google/auth-url'),
  updateGoogleSettings: (d)  => API.put('/google/settings', d),
  disconnectGoogle: ()       => API.delete('/google/disconnect'),
  vapidKey:         ()       => API.get('/push/vapid-public-key'),
  subscribe:        (sub, ua) => API.post('/push/subscribe', { subscription: sub, user_agent: ua }),
  unsubscribe:      (ep)     => API.post('/push/unsubscribe', { endpoint: ep }),
  sendPush:         (d)      => API.post('/push/send', d),
  notifications:    ()       => API.get('/push/notifications'),
  unreadCount:      ()       => API.get('/push/notifications/unread-count'),
  markRead:         (id)     => API.post(`/push/notifications/${id}/read`),
  markAllRead:      ()       => API.post('/push/notifications/read-all'),
  notificationSettings:       () => API.get('/notifications/settings'),
  updateNotificationSettings: (d) => API.put('/notifications/settings', d),
  adminStats:    ()       => API.get('/admin/stats'),
  weightings:    ()       => API.get('/grades/weightings'),
  setWeighting:  (d)      => API.post('/grades/weightings', d),
  storageStats:  ()       => API.get('/admin/storage-stats'),
  cleanupMealplans: ()    => API.post('/admin/cleanup-mealplans'),
  auditLogs:     (p)      => API.get('/admin/audit-logs' + (p ? `?${new URLSearchParams(p)}` : '')),
  chatConversations:    ()          => API.get('/chat/conversations'),
  createChatConversation: (d)       => API.post('/chat/conversations', d),
  chatMessages:         (id, before) => API.get(`/chat/conversations/${id}/messages` + (before ? `?before=${before}` : '')),
  sendChatMessage:      (id, d)     => API.post(`/chat/conversations/${id}/messages`, d),
  markChatRead:         (id)        => API.post(`/chat/conversations/${id}/read`),
  voteChatPoll:         (convId, msgId, optionId) => API.post(`/chat/conversations/${convId}/messages/${msgId}/vote`, { option_id: optionId }),
  setChatMute:          (id, muted) => API.post(`/chat/conversations/${id}/mute`, { muted }),
};
