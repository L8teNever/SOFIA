name: remove mealplan patch helpers

on:
  push:
    paths:
      - .github/workflows/meal-swipe-once.yml

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - name: remove patch helpers
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -r .github/mealpatch
          git rm .github/workflows/meal-swipe-once.yml
          git commit -m "chore: remove mealplan patch helpers"
          git push origin HEAD:main
